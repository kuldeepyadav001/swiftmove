# Optional for deployed frontend if WebSocket is not proxied through the same domain.
# For Railway backend use your real backend domain:
# VITE_WS_URL=wss://YOUR-RAILWAY-BACKEND.up.railway.app/ws

VITE_WS_URL=
